<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express Brains</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>

<body class="d-flex flex-column vh-100">
    <%- include('header') %>
        <section
            class="container mt-5 text-center d-flex justify-content-center flex-column align-items-center flex-grow-1">
            <h1>Classement</h1>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nom</th>
                        <th scope="col">Score</th>
                        <th scope="col">Date</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (scores && scores.length> 0) { %>
                        <% scores.slice(0, 10).forEach((score, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= score.pseudo %>
                                </td>
                                <td>
                                    <%= score.score %>
                                </td>
                                <td>
                                    <%= new Date(score.date).toLocaleString('fr-FR', { day: '2-digit' , month: '2-digit'
                                        , year: 'numeric' , hour: '2-digit' , minute: '2-digit' }) %>
                                </td>
                            </tr>
                            <% }); %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="4">Aucun score enregistré.</td>
                                    </tr>
                                    <% } %>
                </tbody>

            </table>
        </section>
        <%- include('footer') %>
</body>


</html>